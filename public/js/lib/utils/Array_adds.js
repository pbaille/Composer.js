// Generated by CoffeeScript 1.6.3
(function() {
  define(["vendors/ruby"], function() {
    Array.prototype.rotate = (function() {
      var splice, unshift;
      unshift = Array.prototype.unshift;
      splice = Array.prototype.splice;
      return function(count) {
        var len, result;
        result = this.slice(0);
        len = result.length >>> 0;
        count = count >> 0;
        unshift.apply(result, splice.call(result, count % len, len));
        return result;
      };
    })();
    Array.prototype.rotations = function() {
      var result, x, _i, _ref;
      result = [];
      for (x = _i = 0, _ref = this.length - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; x = 0 <= _ref ? ++_i : --_i) {
        result.push(this.rotate(x));
      }
      return _a.uniq(result);
    };
    Array.prototype.median = function() {
      return this.somme() / this.length;
    };
    Array.prototype.somme = function() {
      return this.reduce(function(previousValue, currentValue) {
        return previousValue + currentValue;
      });
    };
    Array.prototype.tonicize = function() {
      var result,
        _this = this;
      result = this.map(function(x) {
        return (x - _this[0] + 12) % 12;
      });
      return _a.sort(result);
    };
    Array.prototype.tonicized_rotations = function() {
      return this.rotations().map(function(x) {
        return x.tonicize();
      });
    };
    return Array.prototype.to_functs = function() {
      return this.map(function(i) {
        if (i) {
          return i % 12;
        } else {
          return i;
        }
      });
    };
  });

}).call(this);
