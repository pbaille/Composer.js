// Generated by CoffeeScript 1.6.3
(function() {
  define(['lib/utils/Array_adds'], function() {
    return describe("Array adds", function() {
      var a;
      a = [];
      beforeEach(function() {
        return a = [1, 2, 3];
      });
      it("rotate", function() {
        return expect(a.rotate(2)).toEqual([3, 1, 2]);
      });
      it("rotations", function() {
        return expect(a.rotations()).toEqual([[1, 2, 3], [2, 3, 1], [3, 1, 2]]);
      });
      it("somme", function() {
        return expect(a.somme()).toEqual(6);
      });
      it("median", function() {
        expect(a.median()).toEqual(2);
        return expect([1, 2].median()).toEqual(1.5);
      });
      it("tonicize", function() {
        return expect(a.tonicize()).toEqual([0, 1, 2]);
      });
      return it("tonicized_rotations", function() {
        return expect(a.tonicized_rotations()).toEqual([[0, 1, 2], [0, 1, 11], [0, 10, 11]]);
      });
    });
  });

}).call(this);
