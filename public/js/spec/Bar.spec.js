// Generated by CoffeeScript 1.6.3
(function() {
  define(["lib/core/structure/Bar", "lib/core/base/RVal", "lib/core/base/Mode"], function() {
    var Bar, Mode, RVal;
    Bar = AC.Core.Bar;
    RVal = AC.Core.RVal;
    Mode = AC.Core.Mode;
    return describe("Bar class", function() {
      var create_instance;
      create_instance = function() {
        var b;
        return b = new Bar({
          beats: 4,
          beat_val: new RVal(1),
          bpm: 120,
          resolution: new RVal(1, 4),
          harmonic_directives: [
            {
              at: new RVal(0),
              mode: new Mode("C Lyd")
            }, {
              at: new RVal(2),
              mode: new Mode("Eb Lyd")
            }
          ]
        });
      };
      it("initialize", function() {
        window.b = create_instance();
        console.log(b);
        return expect(b).toBeTruthy();
      });
      it("duration", function() {
        var b;
        b = create_instance();
        return expect(b.duration().eq(new RVal(4))).toBe(true);
      });
      it("ms_duration", function() {
        var b;
        b = create_instance();
        return expect(b.ms_duration()).toEqual(2000);
      });
      return it("ms_duration_at", function() {
        var b;
        b = create_instance();
        return expect(b.ms_duration_at(new RVal(2))).toEqual(1000);
      });
    });
  });

}).call(this);
