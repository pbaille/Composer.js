// Generated by CoffeeScript 1.6.3
(function() {
  define(["lib/core/base/Degree"], function() {
    return describe("Degree class", function() {
      var d;
      d = {};
      beforeEach(function() {
        return d = new AC.Core.Degree("second", 1);
      });
      describe("constructor", function() {
        it("name", function() {
          return expect(d.name).toBe("m2");
        });
        it("generic_name", function() {
          return expect(d.generic_name).toBe("second");
        });
        return it("dist", function() {
          return expect(d.dist).toBe(1);
        });
      });
      it("alt method", function() {
        var d1, d2;
        d1 = d.alt(1);
        d2 = d.alt(2);
        expect(d1.dist).toBe(2);
        expect(d2.dist).toBe(3);
        return expect(d2.name).toBe("#2");
      });
      it("test_equality", function() {
        var other;
        other = new AC.Core.Degree("m2");
        return expect(d.eq(other)).toBe(true);
      });
      it("dist_up_to", function() {
        var other;
        other = new AC.Core.Degree("m3");
        return expect(d.dist_up_to(other)).toEqual(2);
      });
      return it("dist_down_to", function() {
        var other;
        other = new AC.Core.Degree("m3");
        return expect(d.dist_down_to(other)).toEqual(10);
      });
    });
  });

}).call(this);
