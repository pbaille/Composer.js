// Generated by CoffeeScript 1.6.3
(function() {
  define(["lib/core/composer/harmony/HGen"], function() {
    var HGen, Mode;
    HGen = AC.Core.HGen;
    Mode = AC.Core.Mode;
    return describe("HGen", function() {
      it("initialize without error", function() {
        var hc;
        hc = new HGen("C Lyd");
        expect(hc).toBeTruthy();
        expect(hc.current).toEqual(new Mode("C Lyd"));
        return expect(hc.center).toEqual(new Mode("C Lyd"));
      });
      it("rel_move", function() {
        var hc;
        hc = new HGen("C Lyd");
        hc.rel_move("SD");
        expect(hc.current).toEqual(new Mode("F Lyd"));
        hc.rel_move("SD");
        return expect(hc.current).toEqual(new Mode("Bb Lyd"));
      });
      it("abs_move", function() {
        var hc;
        hc = new HGen("C Lyd");
        hc.abs_move("SD");
        expect(hc.current).toEqual(new Mode("F Lyd"));
        hc.abs_move("SD-");
        return expect(hc.current).toEqual(new Mode("Ab Lyd"));
      });
      it("centerize", function() {
        var hc;
        hc = new HGen("C Lyd");
        hc.abs_move("SD");
        hc.centerize();
        expect(hc.center).toEqual(new Mode("F Lyd"));
        hc.abs_move("SD");
        return expect(hc.current).toEqual(new Mode("Bb Lyd"));
      });
      return it("set_center", function() {
        var hc;
        hc = new HGen("C Lyd");
        hc.set_center("Bb Alt");
        return expect(hc.center).toEqual(new Mode("Bb Alt"));
      });
    });
  });

}).call(this);
